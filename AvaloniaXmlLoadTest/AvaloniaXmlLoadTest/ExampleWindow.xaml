<Window
    x:Class="AvaloniaXmlLoadTest.ExampleWindow"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:testControls="clr-namespace:AvaloniaXmlLoadTest.TestControls;assembly=AvaloniaXmlLoadTest"
    Title="ExampleWindow"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Window.Styles>
        <Style Selector="Button">
            <Setter Property="Height" Value="100" />
            <Setter Property="Width" Value="100" />
            <Setter Property="BorderThickness" Value="2" />
            <Setter Property="BorderBrush" Value="#555526" />
            <Setter Property="Content" Value="SomeContent" />
        </Style>
    </Window.Styles>
    <StackPanel>
        <StackPanel Orientation="Horizontal">
            <Button Content="Button with tip" ToolTip.Tip="Some tip" />
            <Button />
            <Button />
            <Button Content="Button with tip" ToolTip.Tip="Some tip" />
        </StackPanel>
        <Grid ColumnDefinitions="*,*" RowDefinitions="*,*">
            <testControls:TestComboBox Name="tcbList" />
            <testControls:TestTreeBox Grid.Row="1" />
            <Button Grid.Column="1" Background="AliceBlue" />
            <Button Grid.Row="1" Grid.Column="1" />
        </Grid>
    </StackPanel>
</Window>
